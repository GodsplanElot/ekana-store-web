{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.14.0 --activate && cd frontend && pnpm install --frozen-lockfile",
  "buildCommand": "cd frontend && pnpm build",
  "outputDirectory": "frontend/.next"
}
